var books = [];
(function init() {
/*  	alert("geerer");
	$.getJSON("../data.json", function(data) {
		$.each(data, function(i) {
		    alert(i);
		    books.push(data[i]);
		});
	});
	$.get("allbookstemplate.html", function(temp){
		for(var i = 0; i < books.length; i++){
			$.template("markup", temp);
			$.tmpl("markup", books).appendTo("bookstable");
		}
	}); */	 
})();
